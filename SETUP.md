# คู่มือ: เอาแอปขึ้นเว็บ + ให้คนในครอบครัวใช้ร่วมกัน

มี 2 ส่วน ทำครั้งเดียวจบ:
- **ส่วน A** — เอาไฟล์ขึ้นเว็บ (ฟรี) ให้มีลิงก์เปิดได้จากทุกเครื่อง
- **ส่วน B** — เปิดระบบซิงค์ ให้ทุกคนเห็นข้อมูลตรงกัน (แก้ที่เครื่องไหนก็อัปเดตให้เครื่องอื่นทันที)

> ทำ **ส่วน B ก่อนก็ได้** แล้วค่อยทำ A — ไม่จำเป็นต้องเรียงกัน

---

## ส่วน A — เอาขึ้นเว็บด้วย GitHub Pages (ฟรี, ~10 นาที)

1. สมัคร/เข้า [github.com](https://github.com)
2. กด **New repository** → ตั้งชื่อ เช่น `market-bill` → เลือก **Public** → **Create**
3. หน้า repo ที่ว่าง กด **uploading an existing file** แล้วลากไฟล์ทั้งหมดในโฟลเดอร์นี้เข้าไป
   (อย่างน้อยต้องมี `index.html`) → **Commit changes**
4. ไปที่ **Settings → Pages**
   - Source: **Deploy from a branch**
   - Branch: **main** / **/(root)** → **Save**
5. รอ ~1 นาที จะได้ลิงก์แบบ `https://<ชื่อคุณ>.github.io/market-bill/`
6. ส่งลิงก์นี้ให้คนในครอบครัว เปิดในมือถือ/คอมได้เลย (บันทึกหน้าจอ / Add to Home Screen ก็ได้)

**อยากให้เปิดในแอป LINE (LIFF):**
1. [LINE Developers Console](https://developers.line.biz/console/) → สร้าง Provider → **LINE Login channel**
2. แท็บ **LIFF** → **Add** → Endpoint URL = ลิงก์ GitHub Pages ข้างบน → Size = **Full**
3. คัดลอก **LIFF ID** → เปิดแอป → แท็บ **ตั้งค่า** → ช่อง LIFF ID → วาง
4. ลิงก์เปิดในไลน์: `https://liff.line.me/<LIFF_ID>` (เอาไปแปะ Rich menu ได้)

---

## ส่วน B — เปิดระบบซิงค์ (Firebase) ให้ใช้ข้อมูลร่วมกัน

Firebase เป็นบริการของ Google ใช้ฟรีในปริมาณงานเท่านี้สบาย ๆ

### B1. สร้างโปรเจกต์ (ทำครั้งเดียว โดยคนใดคนหนึ่ง)

1. เข้า [console.firebase.google.com](https://console.firebase.google.com) ด้วยบัญชี Google
2. **Add project** → ตั้งชื่อ เช่น `market-bill` → กด Continue ไปเรื่อย ๆ
   (ปิด Google Analytics ได้ ไม่ต้องใช้) → **Create project**

### B2. เปิด Firestore (ที่เก็บข้อมูล)

1. เมนูซ้าย **Build → Firestore Database** → **Create database**
2. เลือก location: **asia-southeast1 (Singapore)** → Next
3. เลือก **Start in production mode** → **Create**
4. ไปแท็บ **Rules** วางข้อความนี้แทนของเดิม แล้วกด **Publish**:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{room}/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

> แปลว่า: ใครก็ตามที่เปิดแอป (ระบบจะล็อกอินให้อัตโนมัติแบบไม่ระบุตัวตน) อ่าน/เขียนห้องได้
> ความปลอดภัยมาจากการที่ **รหัสห้องเดายาก** + ลิงก์เว็บเป็นแบบส่วนตัว

### B3. เปิดการล็อกอินอัตโนมัติ

1. เมนูซ้าย **Build → Authentication** → **Get started**
2. แท็บ **Sign-in method** → เลือก **Anonymous** → **Enable** → **Save**

### B4. คัดลอก config

1. ไอคอนเฟือง ⚙️ (มุมซ้ายบน) → **Project settings**
2. เลื่อนลงหัวข้อ **Your apps** → กดไอคอน **</>** (Web) → ตั้งชื่อเล่นอะไรก็ได้ → **Register app**
3. จะเห็นโค้ดก้อนหนึ่งหน้าตาแบบนี้ — **คัดลอกเฉพาะส่วนในวงเล็บปีกกา** `{ ... }`:

```js
const firebaseConfig = {
  apiKey: "AIza........",
  authDomain: "market-bill-xxxx.firebaseapp.com",
  projectId: "market-bill-xxxx",
  storageBucket: "market-bill-xxxx.appspot.com",
  messagingSenderId: "12345678",
  appId: "1:12345678:web:abcdef123456"
};
```

### B5. เชื่อมต่อในแอป (ทำที่ **ทุกเครื่อง** ที่จะใช้ร่วมกัน)

1. เปิดแอป → แท็บ **ตั้งค่า** → หัวข้อ **ซิงค์ข้อมูลข้ามเครื่อง**
2. วางก้อน `{ ... }` ลงช่อง **Firebase config**
3. ช่อง **รหัสห้อง** พิมพ์ชื่อห้องที่ตกลงกันในครอบครัว เช่น `talad-somsri`
   — **ต้องพิมพ์เหมือนกันเป๊ะทุกเครื่อง** (ใช้ a–z 0–9 - _ เท่านั้น)
4. กด **เชื่อมต่อ / ซิงค์**
   - เครื่องแรก: จะถามว่า “ห้องยังว่าง อัปโหลดข้อมูลในเครื่องนี้ขึ้นไปตั้งต้นไหม?” → กด **ตกลง**
   - เครื่องต่อ ๆ ไป: จะดึงข้อมูลจากห้องมาทับของเดิมให้อัตโนมัติ
5. ป้ายมุมขวาบนจะขึ้น **● ซิงค์อยู่** (สีเขียว)

เสร็จแล้ว — ต่อไปใครกรอกมิเตอร์/แก้บิลที่เครื่องไหน อีกไม่กี่วินาทีเครื่องอื่นจะเห็นเหมือนกัน

---

## ข้อควรรู้

- **แก้คนละอย่างพร้อมกันได้** เช่น คนนึงกรอกมิเตอร์แผง A01 อีกคนกรอก A05 เดือนเดียวกัน → ไม่ทับกัน
- **แก้ช่องเดียวกันพร้อมกันเป๊ะ ๆ** → คนที่กดทีหลังชนะ (เลี่ยงได้ด้วยการแบ่งงานกัน)
- ป้ายขึ้น **● ซิงค์มีปัญหา** → แตะที่ป้ายเพื่อไปดูข้อความ (มักเป็นเน็ตหลุด หรือยังไม่ได้เปิด Anonymous / ยังไม่ได้ตั้ง Rules)
- **Firebase config กับ รหัสห้อง เก็บในเครื่องแต่ละคน** ไม่ถูกอัปขึ้นห้อง — เครื่องใหม่ต้องกรอกเองครั้งแรก
- ปุ่ม **ส่งออกไฟล์สำรอง** ในแท็บตั้งค่า ยังใช้ได้ แนะนำสำรองเดือนละครั้ง
- ปิดซิงค์ชั่วคราวได้ที่ปุ่ม **หยุดซิงค์** (ข้อมูลยังอยู่ในเครื่อง)

## ค่าใช้จ่าย

Firebase Spark plan (ฟรี): อ่าน 50,000 / เขียน 20,000 ครั้งต่อวัน — งานเก็บค่าเช่าตลาดใช้ไม่ถึง 1% ต่อวัน
